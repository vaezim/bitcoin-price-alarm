# Bitcoin price Alarm

Frequently checks the price and plays a sound if price jumps above or drops below a set limit.

## Requirements
```bash
python -m venv .venv
pip install -r requirements.txt
sudo apt install mpg123
```

## Run
```bash
python price_alarm.py
```
